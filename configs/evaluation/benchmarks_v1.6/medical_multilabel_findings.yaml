schema_version: "1.0"
runtime: {seed: 42, device: cpu}
evaluation:
  benchmark: medical_multilabel_findings
  data:
    adapter: medical_tasks_jsonl
    source: jsonl
    path: examples/medical/benchmarks_v1.6.jsonl
    image_root: examples/medical/images
    provenance: examples/medical/benchmarks_v1.6.provenance.json
    benchmark_filter: medical_multilabel_findings
    validate_media: true
    deidentified: true
  model:
    backbone: medical_reference
    parameters: {fixed_answer: '{"labels":["bright focus"]}'}
  protocol:
    name: medical_multilabel_findings
    version: "1.0"
    metric_suite: medical_multilabel_findings
    require_provenance: true
    require_deidentified: true
  mode: full
  resume: false
  output_directory: outputs/evaluation/benchmarks_v1.6/medical_multilabel_findings
